import 'package:flutter/material.dart';

class AdvisorBot extends StatelessWidget{
  const AdvisorBot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body:Center(
          child:Text("Welcome TO Bot Page"),
        )
    );
  }
}