import 'package:flutter/material.dart';

class LearningPath extends StatelessWidget{
  const LearningPath({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body:Center(
          child:Text("Welcome TO Learning"),
        )
    );
  }
}